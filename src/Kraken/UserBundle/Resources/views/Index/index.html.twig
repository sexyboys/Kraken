{% extends "KrakenUserBundle::layout.html.twig" %}

{% block more_css %}


{% endblock %}
{% block more_js %}

{% endblock %}
{% block content %}
    <div class="container">
        <div class="row-fluid">
            <div class="span12 media">

                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                     <h4>{{ "home.welcome"|trans({'%username%': app.user.username}) }}</h4>
                     <br/>
                     <p>
                        {{ "home.scenario"|trans({'%count%':app.user.scenarios|length}) }}
                     </p>
                {% else %}
                    {% include ":Security:login.html.twig" %}

                {% endif %}
            </div>

        </div>
    </div>
{% endblock %}